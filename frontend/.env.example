# Mock API - use seed data for UI development (default: true)
NEXT_PUBLIC_USE_MOCK_API=true

# When backend is ready, set to false and configure:
# NEXT_PUBLIC_USE_MOCK_API=false
# NEXT_PUBLIC_API_URL=https://api.talentos.io/v1
